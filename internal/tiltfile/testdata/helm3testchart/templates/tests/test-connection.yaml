apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm3testchart.fullname" . }}-test-connection"
  labels:
{{ include "helm3testchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm3testchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
