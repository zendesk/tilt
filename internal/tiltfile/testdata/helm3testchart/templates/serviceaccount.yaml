{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "helm3testchart.serviceAccountName" . }}
  labels:
{{ include "helm3testchart.labels" . | nindent 4 }}
{{- end -}}
