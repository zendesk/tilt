apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm3testchart.fullname" . }}
  labels:
    {{- include "helm3testchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm3testchart.selectorLabels" . | nindent 4 }}
